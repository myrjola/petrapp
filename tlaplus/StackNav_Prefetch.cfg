\* TLC config: speculation-rules prefetch ENABLED, no mitigation.
\* Cookie expiration disabled to isolate the prefetch-staleness bug.
\* Expectation: NoStaleSettled VIOLATED -- the same shape the prose
\* analysis predicted. Trace: Prefetch(u2) caches serverToken at time
\* T_a, then SubmitForm rotates serverToken to T_b, then ClickLink(u2)
\* promotes the prefetched response, PromotedSettleNoCheck fires
\* (today's main.js skips the check), docState becomes Loaded with
\* bakedToken = T_a != serverToken = T_b.

SPECIFICATION Spec

CONSTANTS
    Urls = {u1, u2, u3}
    PageLocalUrls = {u3}
    InitialUrl = u1
    MaxHistory = 3
    MaxPosts = 2
    CookieMayExpire = FALSE
    PrefetchEnabled = TRUE
    EagerPagerevealCheck = FALSE

INVARIANTS
    TypeOk
    HistoryAligned
    NoStaleSettled

CONSTRAINT
    StateConstraint
