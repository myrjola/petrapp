# age public keys allowed to decrypt secrets/<app>.env.age (one per line).
# Public keys are NOT sensitive — keep this file committed.
# Add the line printed by `make secrets-keygen`, e.g.:
# age1qz...your-public-key...
